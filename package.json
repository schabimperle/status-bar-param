{
	"name": "status-bar-param",
	"displayName": "Status Bar Parameter",
	"description": "Add selectable parameter to the status bar.",
	"version": "1.2.4",
	"publisher": "mschababerle",
	"icon": "images/icon.png",
	"keywords": [
		"input",
		"status bar",
		"parameter",
		"variables",
		"multi-root ready"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/Schabimperle/status-bar-param"
	},
	"engines": {
		"vscode": "^1.42.0"
	},
	"categories": [
		"Other"
	],
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "StatusBarParam",
			"properties": {
				"statusBarParam.showNames": {
					"scope": "resource",
					"type": "boolean",
					"default": true,
					"description": "Show the name of the parameter before the selected value in the status bar.",
				}
			}
		},
		"commands": [
			{
				"command": "statusBarParam.add",
				"title": "StatusBarParam: Add Parameter to Status Bar"
			}
		]
	},
	"activationEvents": [
		"*"
	],
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^7.0.1",
		"@types/node": "^12.11.7",
		"@types/vscode": "^1.42.0",
		"eslint": "^6.8.0",
		"@typescript-eslint/parser": "^2.18.0",
		"@typescript-eslint/eslint-plugin": "^2.18.0",
		"glob": "^7.1.6",
		"mocha": "^7.0.1",
		"typescript": "^3.7.5",
		"vscode-test": "^1.3.0"
	},
	"dependencies": {
		"jsonc-parser": "^2.2.1"
	}
}
