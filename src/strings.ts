export class Strings {
    static readonly EXTENSION_NAME = 'statusBarParam';
    static readonly COMMAND_ADD = `${Strings.EXTENSION_NAME}.add`;
    static readonly COMMAND_SELECT = `${Strings.EXTENSION_NAME}.select`;
    static readonly COMMAND_EDIT = `${Strings.EXTENSION_NAME}.edit`;
}